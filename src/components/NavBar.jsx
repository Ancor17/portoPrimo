import React from 'react';
import {NavLink} from "react-router-dom";

const NavBar = () => {
    return (
        <div>
            <NavLink>Goods4you</NavLink>
        </div>
    );
};

export default NavBar;